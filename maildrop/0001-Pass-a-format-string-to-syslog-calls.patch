From d2fdfd62122ba307e7a0ee0757a23d8dd5d40368 Mon Sep 17 00:00:00 2001
From: "Brian C. Lane" <bcl@redhat.com>
Date: Tue, 16 Jan 2018 09:43:17 -0800
Subject: [PATCH] Pass a format string to syslog calls

This fixes build errors when using -Werror=format-security
---
 libs/maildrop/maildrop.C | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/libs/maildrop/maildrop.C b/libs/maildrop/maildrop.C
index 10a0c01..89a5ec6 100644
--- a/libs/maildrop/maildrop.C
+++ b/libs/maildrop/maildrop.C
@@ -80,7 +80,7 @@ int	n;
 	{
 		merr << argv[0] << ": " << p << "\n";
 #if SYSLOG_LOGGING
-		syslog(LOG_INFO, p);
+		syslog(LOG_INFO, "%s", p);
 #endif
 		cleanup();
 		return (EX_TEMPFAIL);
@@ -90,7 +90,7 @@ int	n;
 	{
 		merr << argv[0] << ": " << p << "\n";
 #if SYSLOG_LOGGING
-		syslog(LOG_INFO, p);
+		syslog(LOG_INFO, "%s", p);
 #endif
 		cleanup();
 		return (EX_TEMPFAIL);
-- 
2.14.3

